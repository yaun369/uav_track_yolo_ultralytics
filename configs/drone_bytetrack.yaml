# Custom ByteTrack config for relatively small/fast drones.
# Keep tracker_type unchanged per Ultralytics docs.
tracker_type: bytetrack
track_high_thresh: 0.35
track_low_thresh: 0.10
new_track_thresh: 0.35
track_buffer: 40
match_thresh: 0.80
fuse_score: true
